# Setup Scripts

- Linux/macOS:
  - chmod +x setup-appsscript.sh
  - ./setup-appsscript.sh

- Windows (PowerShell):
  - ./setup-appsscript.ps1

The scripts create a new Apps Script project, write .clasp.json locally, and push the repo files.